@model EntityLayer.Concrete.Message
@{
    ViewBag.Title = "NewMessage";
    Layout = "~/Views/Shared/_AdminLayout.cshtml"; // layout kullanıyorsun, onu bırak
}

<!-- HTML <html>, <head>, <body> etiketleri yok! -->
<!-- Sadece sayfa içeriği -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Yeni Mesaj Sayfası</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Home</a></li>
                    <li class="breadcrumb-item active">Compose</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<section class="content">
    <div class="container-fluid">
        <div class="row">
            @Html.Action("MessageListMenu", "Contact")
            <div class="col-md-9">
                <div class="card card-primary card-outline">
                    <div class="card-header">
                        <h3 class="card-title">Yeni Mesaj Oluştur</h3>
                    </div>
                    @using (Html.BeginForm("NewMessage", "Message", FormMethod.Post))
                    {
                        <div class="card-body">
                            <div class="form-group">
                                @Html.TextBoxFor(x => x.ReceiverMail, new { @class = "form-control", placeholder = "Alıcı:" })
                            </div>

                            <div class="form-group">
                                @Html.TextBoxFor(x => x.Subject, new { @class = "form-control", placeholder = "Konu:" })
                            </div>

                            <div class="form-group">
                                @Html.TextAreaFor(x => x.MessageContent, new { @class = "form-control", id = "compose-textarea", style = "height:300px" })
                            </div>
                        </div>

                        <div class="card-footer">
                            <div class="float-right">
                                <input type="submit" name="submitButton" value="Taslaklara Kaydet" class="btn btn-default" />
                                <input type="submit" name="submitButton" value="Gönder" class="btn btn-primary" />
                            </div>
                            <button type="reset" class="btn btn-default"><i class="fas fa-times"></i> İptal Et</button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</section>

@*@section Scripts {
    <script>
        $(function () {
            $('#compose-textarea').summernote();
        });
    </script>
}*@
